//
// Created by Kim on 06/07/2018.
//

#ifndef PLAZA_MATH_H
#define PLAZA_MATH_H

#define Max(x, y) (((x) > (y)) ? (x) : (y))
#define Min(x, y) (((x) < (y)) ? (x) : (y))
#define Abs(x) (((x) > 0) ? x : -x)
#endif //PLAZA_MATH_H
