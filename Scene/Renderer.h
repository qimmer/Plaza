//
// Created by Kim on 23-08-2018.
//

#ifndef PLAZA_RENDERER_H
#define PLAZA_RENDERER_H

#include <Core/NativeUtils.h>

Unit(Renderer)
    Component(RenderPass)

    Component(Renderer)
        ArrayProperty(RenderPass, RendererModelPasses)
        ArrayProperty(RenderPass, RendererModelPasses)
        ArrayProperty(RenderPass, RendererModelPasses)

#endif //PLAZA_RENDERER_H
